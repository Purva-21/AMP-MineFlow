/*
 * Singularity profile configuration (for HPC)
 */
singularity {
    enabled    = true
    autoMounts = true
    cacheDir   = "${HOME}/.singularity/cache"
}

process {
    container = 'docker://purva21/amp-mineflow:1.0.0'
}
